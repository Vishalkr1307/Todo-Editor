const init={
    isAuth:false,
    token:"",
    isLoading:false,
    isError:false,
}

export const reducer=(store=init,{type,payload})=>{

    switch(type){
        default:
            return {...store}

    }
}